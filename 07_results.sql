-- ============================================================
-- PROJECT 1: UNIVERSITY ACADEMIC MANAGEMENT SYSTEM
-- File: 07_results.sql
-- ============================================================

-- 1. CREATE TABLE: RESULTS

CREATE TABLE Results (
    result_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    enrollment_id NUMBER UNIQUE NOT NULL,
    marks_obtained NUMBER(5,2)
        CHECK (marks_obtained >= 0),
    total_marks NUMBER(5,2) DEFAULT 100,
    grade CHAR(2),
    exam_date DATE,

    CONSTRAINT fk_results_enrollment
        FOREIGN KEY (enrollment_id)
        REFERENCES Enrollment(enrollment_id)
);

-- 2. INSERT SAMPLE DATA

INSERT INTO Results
    (result_id, enrollment_id, marks_obtained, total_marks, grade, exam_date)
VALUES
    (1, 1, 85, 100, 'A', DATE '2024-11-15');

INSERT INTO Results
    (result_id, enrollment_id, marks_obtained, total_marks, grade, exam_date)
VALUES
    (2, 2, 78, 100, 'B', DATE '2024-11-15');

INSERT INTO Results
    (result_id, enrollment_id, marks_obtained, total_marks, grade, exam_date)
VALUES
    (3, 3, 92, 100, 'A', DATE '2024-11-15');

COMMIT;

-- 3. VERIFY RESULTS TABLE

SELECT * FROM Results;
